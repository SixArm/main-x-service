## 12. Glossary cross-reference

Public types and their defining section: `Event` §3.1, `EventBuilder` §3.2, `Location` §3.3, `Address` §3.4, `EventCategory` §3.5, `EventStatus` §3.6, `EventAttendanceMode` §3.7, `EventId` / `EventIdScheme` §3.8, `Confidence` §3.9, `MatchResult` / `MatchBreakdown` §3.10, `MatchConfig` §3.11 / §7, `MatchingError` / `Result<T>` §3.12, `Normalizer` §4, `ParsedAddressLine` §4.5, `MatchingEngine` §5, `SimilarityAlgorithm` §6.1, `Scorer` §6. Methods: `deterministic_match` §5.1, `match_events` §5.2, `match_one_to_many` / `rank_one_to_many` §5.3. Concepts: Renormalisation §5.2.2; Strict mode §5.2.3.

---

