//! Entity prelude.
//!
//! Normally this re-exports every generated SeaORM entity for glob
//! import (`use crate::models::_entities::prelude::*;`). This consumer
//! crate has no local entities, so it is intentionally empty.

// Intentionally empty — there are no local entities to re-export.
