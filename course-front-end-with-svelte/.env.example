# Course Service REST API base URL (no trailing slash).
# 8084 is the docker-compose host-port default. 8080 belongs to
# person-service in the Main X Index family.
PUBLIC_API_BASE_URL=http://localhost:8084
