## 16. Open Questions

- **OQ-1**: SVAR DataGrid free tier is GPL-3.0. If this front-end ships in a commercial / proprietary deployment, what license tier do we need? Pro? Enterprise? Decision required before any production deploy.
- **OQ-2**: Should the create route call `check-duplicates` for an inline preview before the actual `create` POST, or rely solely on the 409 round-trip? Round-trip is simpler; preview is friendlier. Operator feedback needed.
- **OQ-3** (updated 2026-08-28): The BFF + httpOnly-cookie + PASETO model in [`../../../agents/share/authentication-sessions.md`](../../../agents/share/authentication-sessions.md) is implemented (T-22): the browser holds only the `__Host-mxi_session` cookie, the SvelteKit server holds the session and attaches a short-lived PASETO server-side, no token in JS, no `localStorage`. Two parts were open; one is now resolved: ~~(a) **CSRF** on mutating browser→BFF calls has no explicit synchroniser token yet — only `SameSite=Lax` backstops it~~ **resolved 2026-08-28 (PRO-H5):** a double-submit `__Host-mxi_csrf` cookie (non-httpOnly, read by `ApiClient` and echoed as `X-CSRF-Token`) is verified by the proxy against the cookie plus an Origin/Referer backstop, per T-22's entry for the exact mechanics. **Still open**: (b) **when the proxied service returns `401`/`403`**, the proxy today just relays the status/body — there is no UI-level redirect-to-`/signin` or session-expiry banner.
- **OQ-4**: Drift policy: per project decision 2026-06-02 we keep API client + types in each front-end project. Revisit when the third sibling front-end ships if drift becomes painful.
- ~~**OQ-5** (2026-06-03): Live integration validation is blocked on two pre-existing issues in `person-service-with-loco`. (a) The `Dockerfile` runtime stage references debian packages (`libpq5`, `libssl3`, `ca-certificates`) that aren't available in `debian:bookworm-slim`'s default repos, so `podman compose build` fails. (b) The crate is library-only — `Cargo.toml` declares no `[[bin]]` target and there is no `src/main.rs`, so `cargo run --release` fails with "a bin target must be available." The Dockerfile separately copies `target/release/person_service`, which `cargo build` never produces.~~ **Resolved 2026-06-04:** the service crate gained `src/main.rs` wiring `Config::from_env → AppState → api::rest::serve`, and the Dockerfile was rebased on `debian:13-slim` with the missing `perl` / `make` / `gcc` / `curl` build-deps added. The container builds and runs end-to-end (`podman build` then `podman run` — see service's [`DEPLOY.md`](../../person-service-with-loco/DEPLOY.md)). ~~6 / 9 Playwright integration tests pass against the live stack; the remaining 3 are test-data interactions with the now-correctly-aggressive duplicate detector.~~ **The test-data half closed 2026-08-29 (PRO-P4)**, reproduced live against a running service + Postgres: `tests/integration/golden-paths.spec.ts`'s fixture generator (`uniqueDob`/`apiCreatePerson`) used a fixed epoch + a small per-record day offset, which kept every fixture inside the same calendar year. The service's real create-time duplicate detector (`check_duplicates_internal`, threshold 0.7) treats every `E2E_*` family as a fuzzy-search candidate for every other (its candidate search tokenizes on `_` and fuzzy-matches per token — see `person-service-with-loco/src/search/mod.rs`), and its DOB grader (`dob_matching::match_birth_dates`) scores same-year pairs 0.50-0.95 even with wholly distinct names — enough, empirically, to false-positive a 409 on fixture creation regardless of the exact day spacing chosen (three independent date schemes were tried and reproducibly still collided, including one across separate Playwright worker generations after this suite's own module-level counter was found to reset on a worker recycle). The durable fix is not a better-tuned date scheme: `apiCreatePerson` now retries a 409 by regenerating both the DOB and the family-name suffix (bounded, 5 attempts), which is robust to the live matcher's exact scoring rather than trying to predict it. **A separate, unrelated, and much larger blocker was found in the same investigation and is NOT closed by this fix**: the page-visit auth guard (PRO-H10, `src/lib/server/session.ts::requireSignedIn`) and the CSRF double-submit check (PRO-H5, `src/routes/api/proxy/[...path]/+server.ts`) — both landed 2026-08-28/29, after this OQ's "6/9 pass" baseline was written — now require a signed-in session for every mutating flow, and this suite never signs in. Confirmed live: with the duplicate-detector collision fixed, FR-3 (both cases), FR-6, FR-7, FR-8, and FR-9 all still fail, but now purely on the guard/CSRF path (a `/persons/new`/`/persons/merge`/`/persons/[id]/edit` navigation redirecting to `/signin`, or a mutating proxy call rejected `403 {"error":"csrf"}`) — reproduced with a live `person-service-with-loco` + Postgres, not inferred. Closing this needs the suite to actually complete a magic-link sign-in against a live `authentication-service` (or a deliberate, explicitly-approved test-mode bypass of the guard/CSRF check) — infrastructure and a security-relevant decision this task's scope (test-data fixtures) does not cover.

**Closed 2026-08-29 (PRO-P32).** Decision (asked, not assumed): a real
magic-link sign-in, not a guard/CSRF bypass. Built and verified live
end-to-end: `examples/compose/authentication-dev.yml`, a new compose
stack running authentication-service in `LOCO_ENV=development` (the
production-mode stacks never log the magic-link URL — SEC-A3 — so
they cannot support this; the file's own header comment covers the
one non-obvious fix it needed, a container-start `sed` patch of
`server.binding: localhost → 0.0.0.0`, since a dev-mode container
listening on its own loopback is unreachable through the compose
port-forward no matter what `ports:` says). A new Playwright `setup`
project (`tests/integration/auth.setup.ts`) drives the real flow —
`POST /api/auth/signup`, poll the container's log for the token
(ANSI-stripped; ANSI codes survive a plain pipe), `page.goto` the
real `/verify` URL — and saves the resulting `__Host-mxi_session` /
`__Host-mxi_csrf` cookies as a Playwright `storageState` the
`integration` project depends on and reuses; `smoke` is untouched (a
project dependency, not a `globalSetup`, so `pnpm test:e2e` never
pays for a live authentication-service). Verified live: signup → log
line → `/verify` → both real cookies present in the saved
`storageState`; a guarded page (`/persons/new`) then rendered its
form instead of redirecting to `/signin`. Found and fixed one
adjacent, previously-latent bug in the same investigation:
`playwright.config.ts`'s `webServer` command baked `PUBLIC_API_BASE_URL`
into the build but never set the BFF's own runtime `PERSON_API_URL`,
so a UI-submitted mutation (going through the server-side proxy) could
silently target this crate's `.env` default (`:5150`, the native
`cargo run` port) rather than whatever live instance
`PUBLIC_API_BASE_URL` actually pointed the direct-REST fixture helpers
at (`:8080`, the podman-compose container) — now both point at the
same base. `bin/e2e` health-checks both services before invoking
Playwright.
- **OQ-6** (2026-06-04, updated 2026-08-01): Container runtime switched from Docker to Podman across all docs and scripts. The `docker-compose.yml` filename is retained because Podman's `compose` sub-command reads it transparently. The **test** compose files were renamed on 2026-08-01: every service crate now carries a `compose.test.yaml` (Compose spec's canonical name) holding just its Postgres, driven by `scripts/test-db.sh`; the three `docker-compose.test.yml` files were removed and their doc references updated in lockstep, as this note required. The dev/prod `docker-compose.yml` files are the remaining rename.
- **OQ-7** (2026-08-03, repo FE-3): The bulk screen cannot offer a working **download** for an export artifact or an import error report. `BulkJobView.download_url` / `errors_url` are opaque artifact-store references — `file://<path>` on the default local backend, `s3://<bucket>/<key>` on S3 — and the service exposes **no HTTP endpoint that serves the bytes**, so neither is fetchable by a browser. The screen therefore renders each reference as plain text with a note, rather than a link that would 404 or a proxy hop that would have to read the service host's filesystem. Closing this needs a service-side change (an owner-scoped, TTL'd artifact-download route, or presigned URLs surfaced in the view — the S3 backend's `presigned_get` already exists but is not exposed), at which point the front-end swaps the text for a link. Until then the operator retrieves the artifact from the service host.
