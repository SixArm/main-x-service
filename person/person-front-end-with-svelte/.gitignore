node_modules
.svelte-kit
build
dist
.env
.env.local
.env.*.local
.DS_Store
*.log
test-results
playwright-report
.vite

# Real session + CSRF cookies from tests/integration/auth.setup.ts
# (PRO-P32) — regenerated on every `pnpm test:integration` run, never
# committed.
tests/integration/.auth-storage-state.json
