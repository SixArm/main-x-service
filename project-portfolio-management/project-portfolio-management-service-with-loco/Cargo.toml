[workspace]

[package]
name = "project-portfolio-management-service"
version = "0.3.0"
edition = "2024"
rust-version = "1.95"
publish = true
default-run = "project-portfolio-management-service"
authors = ["Joel Parker Henderson <joel@joelparkerhenderson.com>"]
description = "Project portfolio management service — plan registry (one recursive plan tree; kind is an optional label, no gate) and full project-management suite: workflows, OKRs, phases, flow, effort, ceremonies, controls, TPC, value realization"
readme = "README.md"
repository = "https://github.com/sixarm/main-x-service/project-portfolio-management-service-with-loco"
license = "MIT OR Apache-2.0 OR BSD-3-Clause OR GPL-2.0-only OR GPL-3.0-only"
keywords = ["portfolio", "project", "product", "program", "project-management"]
categories = ["development-tools", "rust-patterns"]
include = ["src/**/*", "README.md"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace.dependencies]
# `auth` is deliberately absent from this feature list. It is loco's JWT
# support (`dep:jsonwebtoken`, and therefore `rsa` / RUSTSEC-2023-0071),
# and this family issues PASETO v4.public rather than JWT — see
# agents/share/jwt.md. Dropping it removes the vulnerable crate from the
# dependency graph rather than suppressing the advisory.
loco-rs = { version = "1.1.0", default-features = false, features = ["cli", "with-db", "cache_inmem", "worker"] }

[dependencies]
# Keyed integrity MACs with production-grade key handling — one audited
# implementation shared by every service, rather than a copy per crate.
integrity-mac = { path = "../../integrity/integrity-mac-rust-crate" }
# Unkeyed content digests: SHA-256 (FIPS 180-4) and SHA3-256 (FIPS 202).
sha2 = { version = "0.10" }
sha3 = { version = "0.10" }
loco-rs = { workspace = true  }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["float_roundtrip", "arbitrary_precision"] }
tokio = { version = "1.53", default-features = false, features = [
  "rt-multi-thread",
] }
async-trait = { version = "0.1" }
axum = { version = "0.8" }
tracing = { version = "0.1" }
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
regex = { version = "1.13" }
# Embedded full-text search index (see src/search/). Same version as the
# rest of the family, so one index format.
tantivy = { version = "0.22" }
migration = { path = "migration" }
sea-orm = { version = "2.0", features = [
  "sqlx-sqlite",
  "sqlx-postgres",
  "runtime-tokio-rustls",
  "macros",
] }
chrono = { version = "0.4" }
validator = { version = "0.20" }
uuid = { version = "1.24", features = ["v4", "serde"] }
include_dir = { version = "0.7" }
mimalloc = { version = "0.1.52" }
prometheus = { version = "0.13" }

# Canonical matching algorithm — embedded and used directly as the API DTO.
project-portfolio-management-matcher = { path = "../project-portfolio-management-matcher-rust-crate" }

# Offline PASETO v4.public verification against the authentication-service Ed25519 keys (local path; monorepo 0.2 verifier).
# The `fetch` feature adds `Verifier::from_paseto_keys_url` for the boot-time key-set fetch (`PROJECT_PORTFOLIO_MANAGEMENT_PASETO_KEYS_URL`).
authentication-verifier = { path = "../../authentication/authentication-verifier-rust-crate", features = ["fetch"] }

# Durable event bus Phase 3 (BUS-3, following BUS-1's case-service
# reference; agents/share/event-bus.md §5/§8): the relay's real broker
# sink. Optional and behind the `fluvio` feature below so the default
# dependency tree (and every existing deployment's build) is unchanged —
# the relay's `LoggingSink` covers dev/CI with no broker at all.
fluvio = { version = "0.50", optional = true }

[features]
# Off by default. The relay's `EventSink` seam (src/relay.rs) needs no
# feature to build or test; only `FluvioSink` — the real-broker producer
# — lives behind this.
fluvio = ["dep:fluvio"]

[[bin]]
name = "project-portfolio-management-service"
path = "src/bin/main.rs"
required-features = []

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
loco-rs = { workspace = true, features = ["testing"] }
# Name the `TestServer` type in shared request-test helpers.
axum-test = { version = "17.3.0" }
# `#[tokio::test]` + a local ephemeral-port HTTP listener for the boot-time key-set fetch tests.
tokio = { version = "1.53", default-features = false, features = [
  "rt-multi-thread",
  "macros",
  "net",
] }
serial_test = { version = "3.5.0" }
rstest = { version = "0.25" }
insta = { version = "1.48", features = ["redactions", "yaml", "filters"] }
# Mint PASETO v4.public tokens + a matching Ed25519 key set in-process so the extractor.s
# happy path is verified un-gated (no auth-service / network dependency).
ed25519-dalek = { version = "2.2" }
rusty_paseto = { version = "0.10", default-features = false, features = ["v4_public", "serde_json"] }
sha2 = { version = "0.10" }
base64 = { version = "0.22" }
# Temp directories for the search-index tests.
tempfile = { version = "3" }

[[bench]]
name = "service_bench"
harness = false
